events {}

http {
  include mime.types;

  server {
    listen 80;
    root <relative-path-to-your-dist-folder>;

    location / {
      try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|ico|svg|woff|woff2|ttf)$ {
      expires 1m;
    }
  }
}
